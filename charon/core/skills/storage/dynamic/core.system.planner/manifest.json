{
  "package": "core.system.planner",
  "skill_type": "contract",
  "version": "2.0.0",
  "domain": "Orchestration",
  "status": "ACTIVE",
  "description": "Execution envelope for the system_planner role.",
  "author": "Charon System",
  "entrypoint": "plugin.py",
  "system_requirements": [],
  "allowed_agents": [
    "system_planner"
  ],
  "actions": [
    {
      "action_name": "sys_synthesize_execution_plan",
      "skill_id": "wc_planner_workspace",
      "handler_name": "PlannerWorkContract",
      "description": "Default execution envelope for the system_planner. Accepts a raw task payload and contextual tools, returning a structured PlanArtifact DAG.",
      "required_permissions": [
        "contract:execute"
      ],
      "parameters": {
        "type": "object",
        "properties": {
          "user_query": {
            "type": "string",
            "description": "The raw task payload assigned by the Coordinator."
          }
        },
        "required": [
          "user_query"
        ]
      },
      "consumed_artifacts": [
        "UnfulfilledRequirement"
      ],
      "produced_artifacts": [
        "PlanArtifact",
        "DiagnosticArtifact"
      ]
    }
  ]
}