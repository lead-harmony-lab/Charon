{
  "skill_id": "quartermaster_bom_auditor",
  "version": "1.0.0",
  "description": "Parses project assembly BOM CSV files and audits component stock availability against PartVault SQLite inventory to detect shortages.",
  "category": "Execution",
  "author": "Charon Librarian",
  "shelf_tags": ["bom", "kicad", "audit", "inventory_shortage", "csv", "assembly"],
  "system_requirements": ["python3", "sqlite3"],
  "supported_actions": {
    "generate_bom": {
      "description": "Reads project BOM CSV file and audits requested quantities against current PartVault stock levels.",
      "parameters": {
        "project_directory": {
          "type": "string",
          "description": "Directory path containing assembly_bom.csv or bom.csv.",
          "required": true
        }
      }
    }
  }
}