{
  "skill_id": "generalist_query_handler",
  "version": "1.0.0",
  "description": "Handles general conversational and informational queries using local LLM inference engines with discreet concierge persona framing.",
  "category": "Execution",
  "author": "Charon Librarian",
  "shelf_tags": [
    "query",
    "chat",
    "llm",
    "generalist",
    "concierge"
  ],
  "system_requirements": [
    "python3",
    "ollama"
  ],
  "supported_actions": {
    "execution_generalist_answer": {
      "description": "Processes conversational or general knowledge questions via local LLM stream generation.",
      "parameters": {
        "prompt": {
          "type": "string",
          "description": "The target query or conversation prompt.",
          "required": true
        },
        "system_prompt": {
          "type": "string",
          "description": "Optional system prompt override for persona framing.",
          "required": false
        },
        "model_name": {
          "type": "string",
          "description": "Model tag to execute inference with (defaults to llama3.1).",
          "required": false
        }
      },
      "handler": "handle_answer_query"
    },
    "execution_generalist_acknowledge": {
      "description": "Generates a brief formal concierge acknowledgment for user directives.",
      "parameters": {
        "prompt": {
          "type": "string",
          "description": "The user directive to acknowledge.",
          "required": false
        }
      },
      "handler": "handle_acknowledge"
    }
  },
  "allowed_agents": [
    "generalist"
  ]
}
