{
  "skill_id": "cleaner_git_manager",
  "version": "1.0.0",
  "description": "Stages and commits workspace changes to local Git repositories.",
  "category": "System",
  "author": "Charon Librarian",
  "shelf_tags": ["git", "commit", "version_control", "workspace"],
  "system_requirements": ["python3", "git"],
  "supported_actions": {
    "commit_workspace": {
      "description": "Stages all workspace changes and commits them to the local repository.",
      "parameters": {
        "project_name": {
          "type": "string",
          "description": "Target project directory name.",
          "required": false
        },
        "target_path": {
          "type": "string",
          "description": "Explicit filesystem path to the repository directory.",
          "required": false
        },
        "commit_message": {
          "type": "string",
          "description": "Custom commit message. Defaults to 'Automated routine commit by Charon.'",
          "required": false
        }
      }
    }
  }
}