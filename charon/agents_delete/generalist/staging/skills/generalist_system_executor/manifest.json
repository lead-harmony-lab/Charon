{
  "skill_id": "generalist_system_executor",
  "version": "1.0.0",
  "description": "Translates natural language requests into bash commands and executes shell commands safely.",
  "category": "System",
  "author": "Charon Librarian",
  "shelf_tags": ["os", "shell", "bash", "execute", "generalist"],
  "system_requirements": ["python3", "bash"],
  "supported_actions": {
    "execute_system_command": {
      "description": "Translates NL directives to CLI syntax if needed and executes shell tasks.",
      "parameters": {
        "command": {
          "type": "string",
          "description": "The shell command or natural language request to run.",
          "required": true
        },
        "timeout": {
          "type": "number",
          "description": "Execution timeout limit in seconds (defaults to 30.0).",
          "required": false
        }
      }
    }
  }
}